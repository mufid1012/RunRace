# Uploads Directory

This directory stores uploaded images.

## Setup

1. Make sure this directory has write permissions (chmod 755 or 777 on Linux)
2. The .htaccess file allows direct access to image files
